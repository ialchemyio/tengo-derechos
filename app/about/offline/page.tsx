import { AboutOfflineView } from "@/components/AboutOfflineView";

export const metadata = { title: "How offline mode works" };

export default function Page() {
  return <AboutOfflineView locale="en" />;
}
