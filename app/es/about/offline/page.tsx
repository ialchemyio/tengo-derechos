import { AboutOfflineView } from "@/components/AboutOfflineView";

export const metadata = { title: "Cómo funciona sin conexión" };

export default function Page() {
  return <AboutOfflineView locale="es" />;
}
